#include <cstring>
#include<stdio.h>
#include "main.h"
/**
 * _puts - gg
 * @str: gg
 * Return: 0
*/
void _puts(char *str)
{
int len;

for (len = 0; len < strlen(str); len++)
_putchar(strlen[len]);
_putchar('\n');
}
